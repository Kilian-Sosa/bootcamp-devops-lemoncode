services:
  prometheus:
    image: prom/prometheus:v3.14.0
    container_name: monitoring-prometheus-self
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    command:
      - --config.file=/etc/prometheus/prometheus.yml
